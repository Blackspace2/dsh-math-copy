/**
 * Host half of math-copy. The feature is browser-only; this empty Cordis plugin
 * keeps the package in the host loader so DSH can discover its client bundle.
 */
export declare function apply(): void;
